The package boost-scope is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS scope)
    target_link_libraries(main PRIVATE Boost::scope)

or the generated cmake configs via:

    find_package(boost_scope REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::scope)
