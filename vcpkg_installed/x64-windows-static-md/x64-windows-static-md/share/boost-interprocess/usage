The package boost-interprocess is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS interprocess)
    target_link_libraries(main PRIVATE Boost::interprocess)

or the generated cmake configs via:

    find_package(boost_interprocess REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::interprocess)
